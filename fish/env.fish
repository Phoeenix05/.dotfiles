# My own paths
set PATH ~/.pbin $PATH

# System / application paths
set PATH /usr/local/bin $PATH
set PATH ~/go/bin $PATH
set PATH /opt/homebrew/bin $PATH

# - Cargo
set RUSTC_WRAPPER sccache
set PATH ~/.cargo/bin $PATH

# - pnpm
set PNPM_HOME ~/Library/pnpm
set PATH $PNPM_HOME $PATH

